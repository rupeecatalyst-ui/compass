/**
 * @deprecated Retired from production paths — COMPASS uses Catalyst One Customer Gateway.
 * Kept only for historical reference and verifier negative-path checks.
 */

export function resolveHomeLoanIntelligence(): never {
  throw new Error(
    "Local home-loan intelligence is retired. Use Catalyst One COMPASS Customer Gateway.",
  );
}
