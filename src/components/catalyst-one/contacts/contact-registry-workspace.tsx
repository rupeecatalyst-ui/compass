"use client";

/**
 * @deprecated Import ContactsWorkspace from directory-workspace.
 * Retained so historical imports resolve to the unified Party Registry.
 */
export {
  ContactsWorkspace as ContactRegistryWorkspace,
  ContactsWorkspace as ContactMasterWorkspace,
  ContactsWorkspace,
  DirectoryWorkspace,
} from "@/components/catalyst-one/directory/directory-workspace";
