import { RelationshipHeatMap } from "@/mission-control/relationship-heat-map";

/** Mission Control — Relationship Heat Map (executive engagement landscape). */
export default function RelationshipHeatMapPage() {
  return <RelationshipHeatMap />;
}
