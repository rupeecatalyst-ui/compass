import { LenderRegistryAdminWorkspace } from "@/components/catalyst-one/lender-registry-admin/lender-registry-admin-workspace";

export default function AdminLenderRegistryPage() {
  return <LenderRegistryAdminWorkspace />;
}
